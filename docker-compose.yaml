services:
  db:
    image: postgres:15.3
    volumes:
      - ./volumes/postgres/data:/var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: sigat
      POSTGRES_USER: sigat
      POSTGRES_DB: sigat
    ports:
      - "5432:5432"